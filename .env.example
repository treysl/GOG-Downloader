# GOG OAuth -- defaults use the public GOG client (no developer key needed)
GOG_CLIENT_ID=46899977096215655
GOG_CLIENT_SECRET=9d85c43b1482497dbbce61f6e4aa173a433796eeae2ca8c5f6129f2dc4de46d9

# OAuth callback URL (used internally; the manual paste flow bypasses this)
REDIRECT_URI=http://localhost:8080/auth/callback

# Frontend origin for cookie domain
FRONTEND_ORIGIN=http://localhost:8080

# Base path for game downloads inside the container
# Mapped via docker/docker-compose.yml
DOWNLOAD_PATH=/downloads
